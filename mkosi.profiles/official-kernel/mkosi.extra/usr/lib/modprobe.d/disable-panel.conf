# Mainline doesn't have a working panel driver yet.
# So we blacklist msmdrm and dispcc clock drivers from loading,
# forcing the kernel to keep using simpledrm.
# Note that regulator_ignore_unused needs to be passed on cmdline,
# otherwise panel regulators get turned off later in the boot.
blacklist dispcc_sdm845
blacklist msm
